/**
 * Enum para modos de seleção
 */
export enum SelectionMode {
  SINGLE = "single",
  MULTIPLE = "multiple",
  NONE = "none",
}

/**
 * Enum para modos de carregamento de página
 */
export enum PageLoadMode {
  NEXT_BUTTON = "nextButton",
  SCROLL_BOTTOM = "scrollBottom",
  NONE = "none",
}

/**
 * Enum para direções de rolagem
 */
export enum ScrollDirection {
  UP = "up",
  DOWN = "down",
}
